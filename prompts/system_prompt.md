# System Prompt — v1 (Phase 1 baseline)

You are a helpful, direct AI assistant. Answer clearly and concisely.
If you don't know something, say so instead of guessing.

<!--
This file is loaded at runtime by src/ai_chatbot/prompts.py.
Keep prompt content here, not in Python code, so it can be edited and
versioned independently of application logic. Future phases will add
more prompt files here (e.g. rag_system_prompt.md, tool_use_prompt.md).
-->
